// 客户化特效前端处理
// 该文件需要位于固定路径：mis2/custom/vrsr/effects/js/custom_effect.js
// 该文件在产品默认特效处理完成后，自动引入
// 自动引入该文件成功后，会调用initCustomEffectStyle函数
// initCustomEffectStyle定义格式如下，即jQuery插件扩展形式，当前对象为table对象

$.fn.initCustomEffectStyle = function (){
//	confirm($(this).attr('id'));
}